// import { FileConfig } from "@/types/types";

export const onEmojiClick = () =>
  // emojiObject: { emoji: string },
  // config: FileConfig | null,
  // setConfig: React.Dispatch<React.SetStateAction<FileConfig | null>>,
  // setPickerState: React.Dispatch<
  //   React.SetStateAction<{ isOpen: boolean; chosenEmoji: { emoji: string } }>
  // >
  {
    // setPickerState((prevState) => ({ ...prevState, chosenEmoji: emojiObject }));
    // if (config) {
    //   setConfig({
    //     ...config,
    //     bookmark: {
    //       ...config.bookmark,
    //       emoji: emojiObject.emoji,
    //     },
    //   });
    // }
  };
